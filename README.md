# electron_fuse_flipper

A simple CLI tool for allowing quick modification of Electron apps that use the "security" features.

```bash
git clone https://github.com/Kyza/electron_fuse_flipper/
cd electron_fuse_flipper
npm i -g
eff
```
